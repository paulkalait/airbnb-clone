import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    margin: 20,
  },
  textInput: {
    fontSize: 18,
    color: 'black',
    marginBottom: 20,
  },
});

export default styles;
